<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.volunteer.mapper.EmailSendLogMapper">

    <select id="emailUsers"   resultType="com.ruoyi.common.core.domain.entity.SysUser" >
        select
            a.*
        from
            sys_user a
                join sys_user_role b on a.user_id = b.user_id
        where
            b.role_id = '2'
          and user_name like CONCAT('%','','%')
          and given_name like CONCAT('%','','%')
          and external_email like CONCAT('%','','%')
    </select>



</mapper>